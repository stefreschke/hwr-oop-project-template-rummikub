= HWR OOP Lecture Project Template

// TODO: Change headline to better fit your project!

This repository contains a student project created for an ongoing lecture on object-oriented programming with Kotlin at HWR Berlin (summer term 2026).

WARNING: This code is for educational purposes only.
Do not rely on it!

== Feature Liste

// TODO : Add a list of features that your project implements. This can be a simple bullet list or a tables

== Modulstruktur

[source]
----
.
├── core/                        # Domänenlogik (keine Abhängigkeiten nach außen)
├── persistence/
│   ├── persistence-filesystem/  # Persistenzadapter: Dateisystem
│   └── persistence-sql/         # Persistenzadapter: PostgreSQL (via Exposed + Liquibase)
└── applications/
    ├── config/                  # Konfigurationsmodell (AppConfig, ConfigLoader)
    ├── service/                 # Spring Boot REST-Service
    └── cli/                     # Kommandozeilenanwendung (Clikt)
----

== Voraussetzungen

[cols="1,2"]
|===
| Tool | Version / Hinweis

| Java (JDK) | 25
| Kotlin | 2.3.0 (wird über Maven verwaltet)
| Docker | Nur für SQL-Tests erforderlich (siehe <<sql-tests>>)
| (Optional) https://github.com/casey/just?tab=readme-ov-file#installation[just] | Aufgaben-Runner, wrappt Maven-Befehle für einfachere Nutzung (Maven-Befehle können auch direkt mit `./mvnw` ausgeführt werden)
|===

[NOTE]
====
Maven kann auch über den mitgelieferten Wrapper `./mvnw` ausgeführt werden – eine separate Maven-Installation ist nicht zwingend notwendig.
====

== Schnellstart

=== Projekt bauen und testen

[source,bash]
----
just test
----

Führt alle Tests aus.
Docker ist *nicht* erforderlich – SQL-Tests sind initial deaktiviert (siehe <<sql-tests>>).

=== Alle verfügbaren Befehle anzeigen

[source,bash]
----
just
----

.Übersicht der wichtigsten Befehle
[cols="1,3"]
|===
| Befehl | Beschreibung

| `just test` | Alle Tests ausführen
| `just build` | Projekt bauen und ins lokale Maven-Repository installieren
| `just compile` | Nur kompilieren (ohne Tests)
| `just service` | Spring Boot Service starten (erfordert vorheriges `just build`)
| `just cli [args]` | CLI ausführen (erfordert vorheriges `just build`)
| `just start-db` | PostgreSQL-Datenbank starten (erfordert Docker)
| `just stop-db` | PostgreSQL-Datenbank stoppen
| `just discard-db` | PostgreSQL-Datenbank stoppen und alle Daten löschen
| `just mutation-test` | Mutationstests für das `core`-Modul ausführen (PITest)
| `just mutation-test-open` | Aktuellen PITest-Bericht im Browser öffnen
|===

== Arbeiten mit dem Template-Repository (Upstream)

Dieses Projekt ist ein Fork des Template-Repositories.
Das Template bleibt als `upstream` eingebunden, damit Aktualisierungen daraus übernommen werden können.
Die eigene Fork bei GitHub ist `origin`.

=== Schritt 1: Fork erstellen

Öffne das Template-Repository auf GitHub und klicke auf *Fork*.
Damit entsteht eine eigene Kopie unter deinem GitHub-Account.

=== Schritt 2: Fork klonen

[source,bash]
----
git clone https://github.com/DEIN_USER/TEMPLATE_FORK_REPO.git
cd DEIN_REPO
----

Nach dem Klonen ist deine Fork automatisch als `origin` eingetragen.

=== Schritt 3: Template als Upstream hinzufügen

[source,bash]
----
git remote add upstream https://github.com/stefreschke/hwr-oop-project-template-rummikub.git
----

=== Remotes prüfen

[source,bash]
----
git remote -v
----

Erwartete Ausgabe:

....
origin      https://github.com/DEIN_USER/TEMPLATE_FORK_REPO.git (fetch)
origin      https://github.com/DEIN_USER/TEMPLATE_FORK_REPO.git (push)
upstream    https://github.com/stefreschke/hwr-oop-project-template-rummikub.git (fetch)
upstream    https://github.com/stefreschke/hwr-oop-project-template-rummikub.git (push)
....

=== Änderungen aus dem Template übernehmen

Wenn das Template aktualisiert wurde, kannst du die Änderungen wie folgt in deine Fork übernehmen:

[source,bash]
----
# Neueste Änderungen vom Template holen
git fetch upstream

# Template-Änderungen in deinen main-Branch mergen
git merge upstream/main
----

Bei Konflikten müssen diese manuell aufgelöst und dann committed werden:

[source,bash]
----
# Konflikte beheben, dann:
git add .
git commit -m "chore: merge upstream changes"
git push
----

[NOTE]
====
Alternativ zu `git merge` kann auch `git rebase upstream/main` verwendet werden.
Rebase ist idiomatischer, da es eine lineare Commit-History erzeugt – in der Praxis ist es jedoch etwas aufwendiger zu handhaben, vor allem bei Konflikten.
Für dieses Projekt empfehlen wir daher, auf dem eigenen Fork einfach zu mergen.
====

== Docker und PostgreSQL

=== Docker installieren

Docker Desktop kann unter https://www.docker.com/products/docker-desktop/ heruntergeladen und installiert werden.
Nach der Installation muss Docker Desktop gestartet sein, bevor die folgenden Befehle ausgeführt werden können.

=== PostgreSQL-Datenbank starten

Eine lokale PostgreSQL-Datenbank ist über Docker Compose vorkonfiguriert:

[source,bash]
----
just start-db
----

Die Datenbank ist anschließend unter `localhost:5432` erreichbar (Verbindungsdaten siehe `applications/config.json`).

=== PostgreSQL-Datenbank stoppen

[source,bash]
----
just stop-db
----

Um die Datenbank mit einem *frischen Zustand* (alle Daten gelöscht) neu zu starten:

[source,bash]
----
just discard-db
just start-db
----

[#sql-tests]
== SQL-Tests aktivieren

Die Tests, die Docker und PostgreSQL benötigen (`SqlPersistenceTest`, `ServiceSqlTest`, `CliSqlTest`), sind initial mit `@Disabled` annotiert.

Sobald die SQL-Persistenz implementiert ist, kann die Annotation entfernt werden:

[source,kotlin]
----
// @Disabled("Requires Docker")  // <-- diese Zeile entfernen
@Testcontainers
class SqlPersistenceTest { ... }
----

Danach benötigt `just test` eine laufende Docker-Instanz.
